@{
    ViewData["Title"] = "プロフィール編集";
    var returnUrl = ViewData["ReturnUrl"] as string ?? "/MyPage";
}

<h1>プロフィール編集</h1>

<div class="alert alert-secondary">
    プロフィール編集は次のタスクで実装予定です。現在は MyPage 導線のみ先に用意しています。
</div>

<a class="btn btn-primary" href="@returnUrl">MyPageへ戻る</a>
