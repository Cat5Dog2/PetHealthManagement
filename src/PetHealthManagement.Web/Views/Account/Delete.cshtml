@model PetHealthManagement.Web.ViewModels.Account.DeleteAccountViewModel
@{
    ViewData["Title"] = "アカウント削除";
}

<div class="d-flex flex-column gap-3">
    <div class="page-heading mb-0">
        <div>
            <h1>アカウント削除</h1>
            <p class="text-muted">削除後は復元できません。</p>
        </div>
    </div>

    <div class="alert alert-danger mb-0">
        <p class="mb-2">この操作を実行すると、アカウントと関連データが物理削除されます。</p>
        <ul class="mb-0">
            <li>プロフィール情報</li>
            <li>登録済みのペット</li>
            <li>アップロード済みの画像</li>
            <li>今後追加される関連データも復元できません</li>
        </ul>
    </div>

    <div class="surface">
        <dl class="detail-grid">
            <div>
                <dt>表示名</dt>
                <dd>@Model.DisplayName</dd>
            </div>

            <div>
                <dt>Email</dt>
                <dd>@Model.Email</dd>
            </div>
        </dl>
    </div>

    <form asp-action="DeleteConfirmed" method="post" class="action-row">
        <input type="hidden" name="returnUrl" value="@Model.ReturnUrl" />
        <button type="submit" class="btn btn-danger">削除する</button>
        <a href="@Model.CancelUrl" class="btn btn-outline-secondary">キャンセル</a>
    </form>
</div>
